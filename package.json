{
	"name": "xlipboard",
	"private": true,
	"version": "0.0.0",
	"type": "module",
	"scripts": {
		"dev": "tsx setup.ts && concurrently --kill-others \"npx tsx server.ts\" \"npx vite dev --host\"",
		"build": "tsc && vite build",
		"preview": "vite preview"
	},
	"devDependencies": {
		"@types/express": "^4.17.21",
		"@types/socket.io": "^3.0.2",
		"typescript": "^5.2.2",
		"vite": "^5.2.0"
	},
	"dependencies": {
		"express": "^4.19.2",
		"socket.io": "^4.7.5",
		"socket.io-client": "^4.7.5"
	}
}
